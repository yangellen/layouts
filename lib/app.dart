import 'package:flutter/material.dart';
import 'main_tab_controller.dart';

class App extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(title: 'The Apple Cart', home: MainTabController());
  }
}
